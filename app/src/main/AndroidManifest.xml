<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.INSTANT_APP_FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>


    <application
        android:name=".activities.Beacon"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Beacon"
        >
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Beacon">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.DiscoveryActivity"
            android:theme="@style/Theme.Beacon"
            android:exported="false"
            />

        <activity android:name=".activities.SettingsActivity"
            android:theme="@style/Theme.Beacon"
            android:exported="false"
            />

        <activity android:name=".activities.ControlTabActivity"
            android:theme="@style/Theme.Beacon"
            android:exported="false"
            />

        <activity android:name=".activities.CrashActivity"
            android:theme="@style/Theme.Beacon"
            android:exported="false"
            android:process=":crash"
            android:label="CrashActivity"
            android:launchMode="singleInstance"
            android:taskAffinity="app.beacon.crash"
            android:excludeFromRecents="false"
            />

        <activity android:name=".activities.Call"
            android:exported="false"
            android:label="Call"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:showWhenLocked="true"
            android:taskAffinity="app.beacon.call"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            tools:ignore="DiscouragedApi,LockedOrientationActivity" />

        <service android:name=".services.Daemon"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:permission="android.permission.FOREGROUND_SERVICE"
            />
        <service android:name=".services.Call"
            android:exported="false"
            android:foregroundServiceType="phoneCall"
            />

        <receiver android:name=".services.Broadcasts"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>

    </application>

    <queries>
        <intent>
            <action android:name="android.settings.APP_NOTIFICATION_SETTINGS" />
        </intent>
    </queries>
</manifest>